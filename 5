#define _CRT_SECURE_NO_WARNINGS 1
/*编写程序读取输入，读到#停止，报告ei出现的次数*/

#include<stdio.h>

int main()
{
	int ch = 0;
	int count=0;
	while ((ch = getchar()) != '#')
	{
		if ('e' == ch)
			if ('i' == (ch = getchar()))
				count++;
	}
	printf("%d", count);

	return 0;

}
